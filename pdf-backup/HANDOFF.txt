HANDOFF.md -- Session State Tracker
Last Updated: April 8, 2026
Status: READY FOR PHASE 1 BUILD

CURRENT STATE

No code exists yet. This is a greenfield project. Everything needed to build is in
CLAUDE.md.

WHAT'S BEEN DONE

     Three complete manual performance assessments (Periods 1-3) with Claude
     Grading rubric defined and validated across 3 real assessment cycles
     All data sources identified and access methods documented
     Historical baseline data captured (3 periods of Apple Health, Gmail, GitHub)
     iOS + Watch app architecture designed
     File structure defined
     Assessment engine data model specified (Rust structs + Swift pseudocode)
     Health grading algorithm specified with real thresholds from actual data

WHAT NEEDS TO BE DONE NEXT

Phase 1 -- MVP (start here)

1. Create Xcode project with iOS app + Watch app targets
2. HealthKit integration (HealthKitService.swift)
         Request authorization for all 7 metric types
         Query weekly aggregated statistics
         Handle permission denied gracefully
3. Grade calculation engine (AssessmentEngine.swift + GradingRubric.swift)
         Implement health grading algorithm from CLAUDE.md
         Support Grade enum with GPA conversion
         Weighted average for overall grade
4. SwiftData models (Assessment.swift, HealthMetrics.swift, ManualEntry.swift)
         Store assessments with full metric snapshots
         Store manual entries (meal plan adherence, workouts, revenue)
5. Dashboard view (DashboardView.swift + GradeRingView.swift)
         Large circular grade display with color coding
         Category cards with sparkline trends
         Period selector
6. Health detail view (HealthDashboardView.swift)
         Swift Charts for each metric
         Baseline comparison highlighting
         Trend indicators
7. Manual entry views
         Daily meal plan toggle
         Workout log (type + duration)
         Revenue entry (amount + source + client)
8. Watch app -- basic
         Grade ring display
         Health quick view
         Quick action buttons
 Phase 2 -- API Integrations

 9. Gmail OAuth setup -- PKCE flow for ridgecellrepair@gmail.com
10. Gmail scanner -- run queries from CLAUDE.md, deduplicate by company+role
11. GitHub API integration -- PAT-based, query commits/repos/events
12. Full 7-category engine -- all categories graded, not just health
13. Watch complication -- circular gauge with grade letter

 Phase 3 -- Backend + Polish

14. Rust backend (optional) -- Axum server for automated scanning on kokonoe
15. Push notifications -- local notification when weekly report generates
16. Export -- markdown and PDF generation of assessments
17. Trend predictions -- simple linear regression on grade trajectory

KNOWN CONSTRAINTS

      Matt's primary devices: iPhone (current model unknown), Apple Watch (model
      unknown -- needs WatchOS 10+ for latest SwiftUI)
      Matt uses dark mode
      kokonoe (i9-11900K + RTX 3070 Ti) available as backend host via Tailscale
      Gmail MCP server exists at https://gmailmcp.googleapis.com/mcp/v1 but OAuth
      tokens may not transfer to a standalone app -- will need separate OAuth client
      registration in Google Cloud Console
      mmichels88@gmail.com is a separate account, not connected -- would need
      separate OAuth
      Most valuable GitHub repos are private -- PAT with repo scope required
      Apple Health data cannot leave device per HealthKit guidelines
      Matt's financial situation is tight -- no paid API services, keep everything free-tier or
      self-hosted
TESTING STRATEGY

     Unit tests for grading engine (deterministic -- given metrics, expect specific grade)
     Unit tests for Gmail deduplication (Greenhouse sends 2+ emails per application)
     Integration tests for HealthKit (use HKHealthStore.isHealthDataAvailable() guard)
     Snapshot tests for grade ring view (A/B/C/D/F color states)
     Manual testing on Matt's actual devices before any release

ENVIRONMENT

     Xcode 16+ (Swift 6, iOS 17+, watchOS 10+)
     No CocoaPods/SPM dependencies initially -- use native frameworks only
     HealthKit, SwiftUI, SwiftData, Swift Charts, WatchKit, WatchConnectivity
     OAuth libraries: use native URLSession + ASWebAuthenticationSession for PKCE
     Keep bundle ID: com.ridgecellrepair.performancetracker

NOTES FOR CLAUDE CODE

     Matt communicates in very short, terse messages. Single-sentence pivots. No
     preamble expected.
     Matt will say "last time you told me 10 years and I did it in a weekend" -- his velocity
     is extremely high, don't overestimate timelines.
     Always add verbose logging (os.Logger). This is a non-negotiable preference.
     Admin PowerShell is available on Windows if any build scripts are needed.
     The grading rubric is the SOURCE OF TRUTH. If Matt asks to change grading criteria,
     update CLAUDE.md rubric section.
     All historical assessment data in CLAUDE.md (P1/P2/P3 tables) should be seeded
     into the app as initial data so Matt can see his full trajectory from day one.
